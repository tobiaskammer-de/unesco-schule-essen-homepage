name: Deploy Astro site to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    # Täglich um 03:00 UTC (= 04:00 MEZ / 05:00 MESZ):
    # baut die Seite neu, damit datumsabhängige Ansichten (»Nächste Termine«,
    # Kalender-Monatsraster) tagesaktuell bleiben. Die Termine selbst kommen
    # seit Juni 2026 aus src/content/site/termine.yml (Sveltia, kein Google-Feed mehr).
    - cron: '0 3 * * *'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build with Astro
        run: npx astro build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
